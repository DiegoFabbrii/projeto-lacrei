import * as React from "react";
import { Layout } from "../../components/layout";
import { HeadFC } from "gatsby";

const Professional = () => {
  return <Layout></Layout>;
};

export default Professional;

export const Head: HeadFC = () => <title>Profissional</title>;
